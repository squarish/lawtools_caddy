# Copy to .env. Nothing here is secret, but .env is gitignored anyway so that
# the habit holds when a later site needs a DNS API token for wildcard certs.

# Address Let's Encrypt writes to if a certificate is close to expiring and has
# not renewed. Use one you actually read.
ACME_EMAIL=you@example.com
